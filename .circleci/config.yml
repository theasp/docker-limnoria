version: 2

jobs:
  build:
    docker:
      - latupo/circleci-base
    working_directory: ~/build
    steps:
      - setup_remote_docker
      - run: docker build -t test .
      - run: docker run -it test /bin/true
